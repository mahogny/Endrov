Endrov package for Debian. WIP.

Package decomposition:
* .jar-files replaced with a dependency
* system jar file added into path

Plugin decomposition:
* entire plugins + jar-files can be ripped out into separate packages. platform
  dependent plugins needs this

